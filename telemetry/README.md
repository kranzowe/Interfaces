# Telemetry Node

* `ros2 run telemetry telemetry`
* Find the rover's IP address with `tailscale status`
* Open `[rover ip]:5001` in a browser

You may need to refresh the page after a few seconds to get the display to render.